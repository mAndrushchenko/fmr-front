import { makeStyles } from '@material-ui/core/styles'

export const styles = makeStyles({
  root: {
    height: '100%',
    paddingTop: '64px'
  },
  spinner: {
    position: 'fixed',
    left: '50%',
    top: '50%'
  }
})
